apiVersion: 1

providers:
  - name: 'agent-factory-dashboards'
    orgId: 1
    folder: 'Agent Factory'
    folderUid: 'agent-factory'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
